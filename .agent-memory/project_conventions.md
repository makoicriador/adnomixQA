# Project Conventions & Learned Knowledge

This file is append-only knowledge the agents accumulate over time. The
Healer agent appends an entry every time it applies a fix; humans can also
add conventions by hand — both are read by the Generator before it writes
new code.

## Seed conventions

- Prefer `page.getByRole(...)` and `page.getByLabel(...)` over CSS/XPath selectors.
- Page Object classes live in `/pages` and extend `BasePage`.
- Spec files live in `/tests` and are named `<feature-slug>.spec.ts`.
- Tag smoke-critical tests with `@smoke`; tag negative-path tests with `@negative`.
- Timeouts should rely on Playwright's built-in auto-waiting; avoid manual `page.waitForTimeout`.

## Healer log (auto-appended below)

- [2026-09-15T13:35:01.716Z] adxmanager.dev Suppliers index: the search box matches location as separate city/country tokens, not the literal "City, Country" display string — searching "Xiamen, China" (comma included) returns 0 results, but "Xiamen" or "China" alone match. Search on one token at a time.

- [2026-09-15T13:35:01.716Z] adxmanager.dev Suppliers index Status filter: there are two distinct "Apply" buttons with the same accessible name "Apply" — an inner one inside the filter popover (#filterControlGroup) that only stages the selection into a chip, and an outer one (button[type=submit][form=form-suppliers-index]) that actually submits the GET form. Both must be clicked, in that order, or the filter silently has no effect.

  **Superseded 2026-09-17**: re-verified live and this "Add Filter" dropdown + dual-"Apply"-button UI (and the "Status: ..." chip it produced) no longer exists anywhere on the Suppliers index. The app has since been migrated to the same unified Filter-drawer pattern already used by Freight Forwarders/Customs Brokers/Warehouses: a "Filter" button (icon-glyph-prefixed) toggles a drawer (`#form-suppliers-index-drawer`) with Active/Inactive toggle buttons and a single "Update" submit button. `SuppliersPage.filterByStatus` and `test_ServiceProviders_SuppliersIndex.spec.ts` were updated to match; the `statusChip` getter and `clearAllFilters()` helper were removed since their target elements no longer render. This is a good example of why "verified live" findings need periodic re-verification rather than being treated as permanently true — the live app changes independently of this test suite.

- [2026-09-15T13:35:01.716Z] adxmanager.dev left sidebar: top-level accordion headers like "Service Providers" are not <a>/<button> elements (no accessible role), and exact text matching resolves to the wrong node. Use a case-insensitive substring text locator (getByText(/.../i).first()) for sidebar navigation on this app.

- [2026-09-16T00:00:00.000Z] adxmanager.dev Suppliers Create/Edit drawers: buttons and links preceded by a KTUI/Keenthemes icon-font glyph (`<i class="ki-filled ki-...">`) contribute invisible characters to the computed accessible name (Playwright's ARIA snapshot renders it as extra leading whitespace, e.g. "  Edit"). An exact `getByRole(..., { name, exact: true })` match on these times out with 0 matches; a substring match (omit `exact`) works. Plain text-only buttons/links (no icon) match fine with `exact: true`. Confirmed on: the "Add Supplier" button and a row's "Edit" action link.

- [2026-09-16T00:00:00.000Z] adxmanager.dev Suppliers Create/Edit drawers: several elements are rendered twice in the DOM as a desktop/mobile pair with identical accessible names/content — the "Create" and "Save Changes" submit buttons, and the success confirmation banner (`.kt-alert-success`) itself. Always disambiguate with `.first()` rather than assuming a single match; this mirrors the two "Apply" buttons already documented above for the Status filter.

- [2026-09-16T00:00:00.000Z] adxmanager.dev Suppliers Create/Edit forms: "Country" and "Balance Timing" (and Brand, Messaging Platform, calling-country-code) are KTUI "data-kt-select" comboboxes — the real `<select>` stays `class="hidden"` and a generated wrapper (its next DOM sibling, `[data-kt-select-wrapper]`) renders the visible trigger (`[data-kt-select-display]`) plus a `<li role="option">` listbox (`[data-kt-select-dropdown]`). Playwright's `selectOption()` cannot be used since the native element is never visible; click the display to open it, then click the matching `role=option`. Only some instances enable a search box (Country does; Balance Timing does not) — check for `input[placeholder="Search..."]` before filling it rather than assuming it's always present. See `SuppliersPage.selectKtOption`.

- [2026-09-16T00:00:00.000Z] adxmanager.dev Suppliers Create form: two fields have no "*" in the UI but are effectively required — "Balance Timing" fails gracefully server-side ("The balance payment timing field is required."), while "City" is a NOT NULL database column with no validation rule behind it and 500s with a raw, unhandled Laravel debug page (leaking the SQL query, stack trace, internal DB host/port, and request cookies) if left blank on create. This looks like an application bug and a security exposure (debug mode enabled) worth fixing upstream; the test suite always supplies `city` and treats only Balance Timing as the negative-path case, since asserting the crash page as "expected" would bake in the bug.

  **Extended 2026-09-17**: the bug is not specific to the Create path — clearing an existing supplier's City via Edit and saving crashes with the identical unhandled 500 (`SQLSTATE[23502]`, not-null violation on `"suppliers"."city"`), this time via the PUT `.../suppliers/{id}` update path (`app/V2/Http/Actions/Supplier/Update.php:71`) instead of the POST create path. Any write leaving City blank crashes, not just creation. Reproduced in `test_ServiceProviders_Suppliers_Edit_CityError.spec.ts`, a standalone sibling of the Create-path repro spec — exclude both from routine full-suite runs for the same reason (asserting the crash as "expected" would bake the bug in).

  Also checked and ruled out while investigating this: Warehouses' "City" field (also unmarked "*" in the UI) is genuinely nullable — leaving it blank creates successfully with City rendering as "—", no crash, no error. Same result for Warehouses' "State", "Zip Code", and "Extension Number" (all unmarked, all genuinely optional). Freight Forwarders' and Customs Brokers' "City" fields are both marked "*" and fail gracefully (confirmed by their own existing Create-suite negative-path tests passing). So the NOT-NULL-with-no-validation bug is specific to Suppliers' "city" column — it is not a shared framework-level issue across all four Service Providers sections.

- [2026-09-19T00:00:00.000Z] **Service Providers kebab-menu Delete/Deactivate, confirmed live via `test_ServiceProviders_{Suppliers,CustomsBrokers}_Deactivate.spec.ts` and `test_ServiceProviders_{FreightForwarders,Warehouses}_Delete.spec.ts`**: re-verified the 2026-09-16 per-section kebab table above still holds — **Suppliers and Customs Brokers have no hard "Delete" action at all, only a "Deactivate"/"Activate" status-toggle link** (a plain `<a href=".../toggle-status">`, no `confirm()` dialog); **Freight Forwarders and Warehouses have a hard "Delete" button** behind a native `confirm()` dialog (Warehouses' additionally offers its own separate "Deactivate"/"Activate" toggle alongside Delete — the only section with both). Since Suppliers/Customs Brokers have nothing to "delete", their coverage was written as `_Deactivate.spec.ts` files exercising the toggle round-trip instead of a `_Delete.spec.ts`.

  - **Suppliers deactivate**: redirects from the index straight to the record's own detail page (`.../suppliers/{id}`, no `/edit`), banner "Successfully deactivated the supplier." rendered there; reactivating (kebab link now reads "Activate") does the same redirect with "Successfully activated the supplier.".
  - **Customs Brokers deactivate**: stays on the plain index URL (no redirect), banner "Successfully deactivated the customs broker." / "Successfully activated the customs broker." rendered there instead — a genuine difference from Suppliers' redirect-to-detail behavior, not assumed to match.
  - Both sections' **default (unfiltered) index view only lists Active records** — a deactivated record disappears from the plain search immediately and only reappears via the Filter drawer's "Inactive" toggle. Confirmed by directly checking for the "No suppliers/customs brokers found" empty-state text, not by a `rowsContaining(...).count()` — that empty state itself renders inside a real `<tr>` echoing the searched term back (e.g. "No suppliers found matching \"X\""), which makes a plain substring-match row-count check on the search term false-positive as if a row were still "found". Any new negative-search-result assertion in this app should check the empty-state text, not row count.
  - **Freight Forwarders Delete**: fires `confirm("Are you sure you want to delete this freight forwarder?")`; on accept, redirects to the plain index with "Freight forwarder deleted successfully.", and the record vanishes from the default search (same empty-state-text caveat as above). **Warehouses Delete**: fires `confirm("Are you sure you want to delete this warehouse? This action cannot be undone.")` (longer message than Freight Forwarders'); on accept, redirects to the plain index with `Warehouse "{name}" deleted successfully.` — the only one of these four banners that echoes the record's own name back.
  - **Quirk/bug, both Freight Forwarders and Warehouses**: after a "successful" Delete, the record's own detail page is still reachable at the exact same URL and still returns HTTP 200 with the *complete pre-delete data intact* — status still "Active", Edit/Delete (and, for Warehouses, Add Location) buttons all still present — and with **no "Deleted" indicator anywhere on the page**. This is a different (and arguably worse) flavor of the same class of issue as Products' own soft delete: Products at least renders a "Deleted" badge on the surviving detail page, so these two sections' Delete is even less discoverable as "not really deleted" than Products' already-quirky soft delete. Not asserted as a blocking failure here (no crash, no 5xx) — documented and regression-checked as the confirmed-live behavior instead, same reasoning as the other non-crashing quirks in this file. Dismissing either confirm() dialog leaves the record completely untouched (no navigation, no deletion) — confirmed live for both sections.

- [2026-09-18T00:00:00.000Z] **Site-wide "Filter" drawer, confirmed via `test_Adnomix_Global_FilterCheck.spec.ts`**: crawled every page reachable from https://adxmanager.dev/v2/home for a `[data-kt-drawer-toggle$="-index-drawer"]` button — 24 pages have one (list in `tests/utils/globalFilterPages.ts`), all sharing the same KTUI drawer + Alpine.js mechanics (a "Filter" button — icon-glyph-prefixed, and reads "Filter Applied" instead of "Filter" on Order Payments when its default filter is active — toggling a `#form-<page>-index-drawer` with a single "Update" submit and a "Reset" button whose `onclick` target is *not* always the bare URL: Order Payments resets to `?tab=order&filters[balance]=outstand`, Warehouses Inventory to `?clear_selection=1`, Sales Transactions to a dynamically-generated `?selectedMonth=<current>&selectedYear=<current>`).

  **Major bug, confirmed on 13 of the 24 pages**: putting an unexpected/unrecognized value into a filter's query param and loading the URL directly crashes with an unhandled 500 instead of being validated or silently ignored — the same class of bug as the Suppliers City crash above, but reachable through simple URL manipulation rather than a form field, and far more widespread. Confirmed crashing: **Suppliers, Freight Forwarders, Customs Brokers, Warehouses, All Inventory, All Products, Brands, Categories, Warehouses Inventory, Corporations, Integrations, Users, Error Logs**. Confirmed handling it gracefully (page loads normally with the bogus value simply ignored/producing zero results): **Dashboard, Ready To Ship, Orders, Shipments, Sales, Sales Transactions, Order Payments, Export Sales, Warehousing Costs, Stock Transfers, Inventory Value**. The crashing set spans both checkbox-driven status filters and select-driven ID filters (e.g. Error Logs' `module_id`), so this isn't limited to one field type or one underlying enum column — it looks like a broadly-missing "validate filter value before querying" step in the shared filtering code, not a one-off. This is now a permanent regression check in `test_Adnomix_Global_FilterCheck.spec.ts`, not a standalone excluded repro spec, since fixing it page-by-page is expected and the test should start passing as each page is patched.

  Also noted: `/v2/reports/sales` ("Sales") is unusually slow to render (40-50s+ even for a simple drawer open/submit round trip) — worth a performance look independent of the above; it is not part of the crash list.

- [2026-09-18T00:00:00.000Z] **adxmanager.dev Products > All Products, confirmed via `test_Products_AllProductsIndex/_Create/_Edit/_Delete.spec.ts`**: the "Add Product"/"Edit Product" drawer (`#create-product-drawer`/`#edit-product-drawer`) is far larger than any Service Providers section's — Product Details, Target stock levels (days), Specifications, Fulfillment/3PL (with per-marketplace Amazon/Walmart/Flexport sub-tabs), Import Duties & Parts, Regulatory Requirements — but only **3 fields are actually required server-side**: Product Name, SKU, and Select Supplier, discovered by submitting the drawer fully blank and reading back the exact validation messages rather than trusting the UI's own (sparser) "*" markers. Other quirks confirmed live and encoded in `pages/ProductsPage.ts`:
  - "List Price"/"Retail Price" (`list_price`/`sales_price`) each render **three times** in the same drawer (Product Details plus the Amazon/Walmart Fulfillment sub-tabs share one Alpine `x-model`) — `.first()`, same duplicate-markup discipline as elsewhere in this app.
  - "Select Supplier" (`select[name="supplier_id[]"]`) is a KTUI multi-select backed by ~37 real suppliers with **no search box** (unlike Country elsewhere), and its dropdown is taller than the drawer's own viewport — `option.scrollIntoViewIfNeeded()` is required before `.click()` or Playwright's actionability check times out waiting for the option to be "stable" even though it's already `display:block`.
  - The sidebar accordion is labelled "Products" (not "All Products"); same navigation quirk as `navigateToSuppliers` — an anchored exact match (`/^Products$/i`) resolves to a zero-size/hidden span and silently no-ops, a non-anchored substring (`/Products/i`) is required. See `HomePage.navigateToAllProducts`.
  - The Filter drawer's Status checkboxes are visually hidden behind KTUI's custom styling (same as Warehouses/Customs Brokers — the *label* must be clicked, not the `<input>`), "Active" is pre-checked by default (same pre-checked-by-default trap as Customs Brokers), **and** the visible label for `value="Inactive"` is "Discontinued", not "Inactive" — a filter helper that clicks by the status value directly (rather than a label lookup table) would silently miss the real control.
  - **Bug**: submitting a SKU that already belongs to another product crashes with an unhandled 500 on `POST /v2/products` instead of a graceful "has already been taken" message — same class of bug as the Suppliers blank-City crash. Not exercised by the routine Create suite for the same reason that bug's own repro is kept standalone (asserting a known crash as "expected" bakes it in).
  - **"Delete" is a soft delete, not a hard delete or a Deactivate-style status toggle**: confirmed live that after accepting the native `confirm()` dialog ("Are you sure you want to delete this product? This action cannot be undone.") and getting the "Product deleted successfully." banner, the product's own detail page still returns HTTP 200 at the exact same URL — full data intact, a "Deleted" badge shown, Edit/Delete/Copy still present — it never 404s. It only disappears from the index's default listing/search; the Filter drawer's Status options are only Active/Discontinued, with **no way to filter or list "Deleted" items from the UI at all**. Dismissing the confirm() dialog leaves the record completely untouched (verified: no navigation, no deletion).
  - **Correction (same day, after re-investigation)**: a row's kebab-menu "Delete" was initially logged above as unreliable/broken — that was wrong, and the mistake is worth recording since it came from testing for the wrong confirmation mechanism. The detail page's own "Delete" is a plain button gated by a native `window.confirm()`, disabled outright when the product has history (confirmed: enabled on a freshly created fixture, `disabled` on an established product with real order data). The row's kebab-menu "Delete" looks identical but is a *different* implementation with no such disabled/history guard: it does `$dispatch('confirm-product-delete', { name, action })`, caught by a `@confirm-product-delete.window="openDeleteProduct($event)"` listener that opens a custom, plain-utility-class modal (`div.fixed.inset-0.z-50` with its own "Cancel"/"Delete" buttons — no `role="dialog"`, no `.kt-modal`, no `id*="modal"`, so a generic "look for anything modal-shaped" selector misses it entirely, and listening for a native `dialog` event never fires for it either). Re-tested end-to-end (create fixture -> kebab Delete -> the *modal's own* "Delete" button -> "Product deleted successfully." -> gone from search) across 3 separate rows including one where the detail-page button was disabled, and the kebab path worked correctly every time. `ProductsPage` still standardizes on the detail-page button for automation (simpler single mechanism), but not because the kebab path is broken — it isn't. The real, still-open oddity worth a design/consistency look: the kebab-menu Delete has no visible disabled state for a product with order history, unlike the detail page's equivalent button — a user could reach the confirmation modal for a product the app otherwise refuses to let you delete from its own detail page (untested whether the actual delete request would then also succeed or get rejected server-side).
  - Intermittent same-origin 502s were observed twice during this suite's development: on `/storage/marketplace_image/*.jpg` thumbnails (a product detail page's Amazon/Walmart tabs), and once on `GET /v2/products` itself when 4 test files ran as parallel Playwright workers (4 simultaneous authenticated sessions hammering the same page). Not reproducible running the same suites with `--workers=1`, so this reads as the live server struggling under concurrent automated load rather than a deterministic app bug — worth a look if it starts showing up in CI (which already caps at `workers: 2`), but not treated as a suite-blocking regression here.

- [2026-09-16T00:00:00.000Z] adxmanager.dev Suppliers index: the "Location" column renders "{state}, {country}" — city is not shown there at all. The Suppliers Index suite's own location-search test still passes only because several pre-existing Chinese suppliers happen to have a city name entered into their State field.

- [2026-09-16T00:00:00.000Z] adxmanager.dev Service Providers Create/Edit forms (Suppliers, Freight Forwarders, Customs Brokers): despite the near-identical UI kit (same drawer/KTUI-select mechanics, same `.kt-alert-success`/`.kt-alert-destructive` banner classes, same icon-glyph accessible-name quirk, same duplicate-button pattern), each section has its own distinct, independently-required-fields, redirect, and Location-format behavior — **verify each one live rather than assuming it matches a sibling section**. Confirmed differences:

  | Behavior | Suppliers | Freight Forwarders | Customs Brokers |
  |---|---|---|---|
  | Hidden-required field (no "*" but required) | Balance Timing (graceful) | none found | none found |
  | Field that 500s ugly if blank | City (NOT NULL, no validation rule) | none found | none found |
  | Cell/phone number required? | Phone required | Cell Number optional | Cell Number required |
  | On successful create, stays on... | index | **new record's own detail page** | index |
  | Index "Location" column format | "{state}, {country}" | "{city}, {country}" | "{city}, {state}, {country}" |
  | Row-level kebab menu has "Edit"? | yes (`<a href=".../edit">`, full navigation) | yes (`<a href=".../edit">`, full navigation) | **no** — only "Deactivate"; Edit is a button on the detail page (`#edit-button`, client-side drawer, no navigation) |
  | Row-level kebab menu has "Deactivate"/"Delete"? | Deactivate (soft toggle) | **Delete** (hard delete, native `confirm()` dialog) | Deactivate (soft toggle) |

  `CustomsBrokersPage.openEditFor` therefore clicks into the row's detail page first rather than reusing the kebab-menu pattern from `SuppliersPage`/`FreightForwardersPage`. A cleanup script that assumes every section's kebab menu has a "Deactivate" link will hang on Freight Forwarders — it only has "Edit" and "Delete".

- [2026-09-17T00:00:00.000Z] adxmanager.dev Freight Forwarders/Customs Brokers/Warehouses indexes: unlike Suppliers' "Add Filter" dropdown + two identically-labelled "Apply" buttons, these three sections instead use a "Filter" button (icon-glyph-prefixed, substring match needed) that toggles a drawer (`#form-<section>-index-drawer`) with Active/Inactive toggle buttons and a single "Update" submit button. Confirmed live that Customs Brokers' "Active" toggle is **pre-checked by default** even with no filter applied (Warehouses/Freight Forwarders default to both unchecked) — a `filterByStatus` helper that blindly clicks every requested status (assuming it starts unchecked) silently *unchecks* Active on Customs Brokers instead of checking it, corrupting the filter. Each page object's `filterByStatus` instead reads every checkbox's actual `isChecked()` state first and only clicks a toggle when it doesn't already match the desired state.

- [2026-09-17T00:00:00.000Z] adxmanager.dev Warehouses Create/Edit form: `min_stock_level` and `max_stock_level` carry no "*" in the UI but are BOTH required server-side — a hidden-required-fields bug in the same spirit as Suppliers' Balance Timing/City. Unlike every other required field on this form (which fails with a specific "The X field is required." message), omitting either stock-level field alone still fails the whole create, but only with a generic "Failed to create warehouse. Please try again." banner — the real validation error is swallowed. Confirmed as a 302 redirect with a flashed banner, not an unhandled 500 (unlike Suppliers' City bug), so it's safe to assert on directly as a regression guard rather than needing a separate bug-repro spec. Also confirmed live: Warehouses has no Bank Accounts section at all (present on Suppliers/Freight Forwarders/Customs Brokers), but uniquely has an "Add Location" feature — a client-side modal (`#warehouse-location-modal`, reachable only from a warehouse's own detail page) whose fields are all native HTML5 `required` rather than server-validated, so a blank-required-field submission never round-trips to the server at all (no `.kt-alert-destructive`, just the browser's own constraint validation blocking the click).

- [2026-09-19T00:00:00.000Z] Full-suite run (`npx playwright test --workers=2`, 174 tests) plus a targeted live audit of every Create/Edit form's FULL field set, requested explicitly because the existing Create/Edit specs each only assert a subset of the fields their own `buildFake*Data()` factories fill in. Result: 151 passed, 21 failed, 2 did not run. None of this changes the routine-suite expectations already documented above; breakdown:

  - 13 of the 21 failures are the already-documented site-wide filter-crash bug (same 13 pages as the 2026-09-18 entry). 1 failure (Products Index "creates a product and finds it via search") is the already-documented intermittent 502 on a marketplace_image thumbnail. No change in status for either.
  - 2 failures (Sales "Filter button opens...", Warehouses Inventory "Filter button opens...") were timing/load related, not confirmed new app bugs — Sales timed out on its own Reset button after its already-documented 40-50s render time; Warehouses Inventory hit a bare 30s `page.goto` timeout. Both happened while this run's 2 workers shared the live site with two extra concurrent live-verification scripts (see below) — re-run in isolation before treating either as a real regression.
  - 1 failure + 2 "did not run" (Warehouses Edit "updates a warehouse's contact details" and its 2 cascaded serial-mode siblings): a 15s timeout waiting for the fixture row's kebab-menu toggle on the Warehouses index — most likely the same concurrent-load cause (a Warehouses live-verification script was creating/editing warehouses on the same index at the same time). Needs a clean, isolated re-run to confirm whether `WarehousesPage.openEditFor`'s row lookup has a real flake.
  - 4 failures, all of `tests/user-login.spec.ts`: this file is dead scaffold code, not a real test of this app. It was auto-generated by the framework's own "Generator agent" from a placeholder test plan and never finished — it passes each test's own English scenario description ("a registered user", "they submit valid credentials") as the literal email/password values, its assertion is `expect(page).toHaveURL(/.+/)` (matches any URL), and every test body still has `// TODO: replace with a real assertion for this feature's UI`. Worse, `pageObject.goto('/')` resolves against Playwright's `use.baseURL`, which reads `process.env.BASE_URL` — and `.env`'s `BASE_URL=https://example.com` (a leftover scaffold default, distinct from `ADX_BASE_URL=https://adxmanager.dev`, which every real Page Object uses directly). So this spec never navigates to adxmanager.dev at all; it loads `https://example.com/` and times out on `getByLabel(/email/i)`. Recommend deleting this file and `pages/UserLoginPage.ts` (or rewriting against `LoginPage.ts`'s real selectors) — until then it should not count toward "all tests passed."

- [2026-09-19T00:00:00.000Z] Live field-round-trip audit, Suppliers/Freight Forwarders/Customs Brokers (fresh fixture per section via `buildFake*Data()` with every field populated, checked against the record's own detail page): Freight Forwarders — all 12 collected fields (`companyName, address, city, state, country, zipCode, contactName, telNumber, email, extensionNumber, cellNumber, fax`) reflect correctly verbatim, including after an Edit-path re-check of `zipCode`. Customs Brokers — `companyName/address/city/state/zipCode/country` (combined into one address line), `contactName`, `email`, `telNumber` ("Telephone Number"), `cellNumber` ("Cellphone Number" — label differs slightly from the form's "Cell Number", cosmetic only), and `extensionNumber` all reflect correctly.

  - **Bug: Suppliers' own detail page never displays City, State, or Country as visible text anywhere on the page** (checked Contact Information, Payment Terms, Operations, Audit Log). Data integrity is fine — the raw page HTML does contain the correct values, but only as the `value="..."` attribute of the hidden `#edit-supplier-drawer`'s `#city_edit`/`#state_edit` inputs, never rendered as visible text on the read-only detail view. The values ARE visible elsewhere (the index row's "Location" column, already documented above) — just never on the record's own detail page. Repro: Suppliers → Add Supplier → fill all fields → Create → open the new supplier's own detail page (not Edit, not the index) → city/state appear nowhere in the visible layout.
  - **Bug: Customs Brokers' "Customs Notes" field is saved correctly but never displayed anywhere on the broker's detail page**, before or after editing — confirmed absent even from the raw page HTML, while reopening the Edit drawer's own textarea correctly shows the current/updated value (the save did succeed server-side). Repro: Customs Brokers → create or edit a broker, fill/change "Customs Notes" → save → view that broker's detail page → notes appear nowhere on it.
  - No crashes, 5xx responses, or uncaught JS exceptions were observed for any of the three sections during this audit.

- [2026-09-19T00:00:00.000Z] Live field-round-trip audit, Warehouses: all 14 fields collected by `buildFakeWarehouseData()` (`companyName, status, address, city, state, country, zipCode, contactName, telNumber, extensionNumber, email, shippingSpeed, minStockLevel, maxStockLevel`) reflect correctly on the detail page on create, including `minStockLevel`/`maxStockLevel` under correctly-labeled, non-swapped headings. Re-verified after an Edit changing `minStockLevel`/`maxStockLevel`/`shippingSpeed` — all three round-trip correctly, and the Audit Log correctly logs a new "Warehouse Updated" version entry. No bugs found in Warehouses' own Create/Edit round-trip.

  - **New, separate finding: the `/v2/home` dashboard deterministically 404s roughly 18 `storage/marketplace_image/*.jpg` thumbnails on every load** (reproduced twice) — e.g. `amazon.found.b08nd3989y.jpg`, `ab-us-sf100-bn.jpg`. Distinct from the already-documented *intermittent* 502s on product-detail marketplace images under concurrent load (2026-09-18 Products entry) — this is a deterministic 404 on the home dashboard widget specifically, suggesting missing/broken asset files rather than a load/concurrency issue.

- [2026-09-19T00:00:00.000Z] Live field-round-trip audit + computed-math exploration, Products: with every optional field forced (`brand`, `category`, `skuAliases`, `barcode`, `manufacturingPrice`, `description`), `productName/brand/supplier/sku/listPrice/retailPrice/manufacturingPrice/category/description` all reflect correctly on the detail page — including `manufacturingPrice` ("Manufacturing Price $5.50"), never previously asserted despite an existing spec setting it. `listPrice`/`retailPrice` show correct `$` formatting and are not swapped with each other.

  - **Bug: `skuAliases` and `barcode` save correctly server-side (both pre-fill correctly when the Edit drawer is reopened) but are never rendered anywhere on the product's own detail page** — same class of issue as the Suppliers city/state/country gap above.
  - **Test-framework bug (not a live-app bug) in `pages/ProductsPage.ts`**: `fillForm`'s handling of `category` calls `selectKtOption()` (the KTUI-combobox helper correctly used for Brand/Supplier), but `<select name="category_id">` has no `data-kt-select` attribute and no `data-kt-select-wrapper` sibling at all — unlike Brand/Supplier it's a plain native `<select>` (`class="kt-select"` is styling only). Calling `selectKtOption()` on it hangs ~30s waiting for a wrapper that never renders — almost certainly why no existing Create/Edit spec exercises `category`. Fix needed in `ProductsPage.fillForm` (use plain `.selectOption({ label })` for this field), not in the app.
  - **Computed field confirmed correct: "Import Duties & Parts" → single-product "Total"** (Alpine `dutyTotal()`) = `dutyAmount + tariffAmount`, each amount either a Fixed $ value or `percentage × Import Price`. Verified across Fixed/Percentage combinations (Import Price $100 + Duty 10% + Tariff 5% → Total $15.00; Duty Fixed $20 + Tariff 5% → Total $25.00; both Fixed $20+$7 → Total $27.00; both blank → Total $0.00, no NaN) — correct in every mode tested, and persists/re-renders correctly on the read-only detail page.
  - **Client-side live-preview-only display bug — corrected 2026-09-20 after further verification.** Per-part "Duty Rate" shown live in the Add/Edit Product drawer (when "Does this product have parts?" = Yes) stays at $0.00 while a part's Duty/Tariff rate type is set to "Fixed" and a value is entered (Percentage mode updates the live preview correctly). **However, saving and then checking the Product View/detail page shows the correct value**: filling a part with cost $30, Duty Fixed $4, Tariff Fixed $2, then clicking Save Changes and reloading the product's detail page, shows the read-only "Import Duties & Parts" table with "Fixed: $4.00" / "Fixed: $2.00" and a correct Duty Rate of $6.00, with the Total also $6.00. So this is scoped to the in-drawer live preview only, during ongoing creation/editing — the underlying data is saved and computed correctly server-side and displays correctly everywhere after saving. (Superseded finding from 2026-09-19: that entry mischaracterized this as a persisted-data arithmetic bug corrupting the saved aggregate total — it does not; the save/display path is correct. Likely cause of the live-preview glitch: the drawer's computed-preview logic probably only reacts to the Percentage-mode fields, not `duty_fix_rate`/`part_tariff_fix_rate`.)
  - No other computed/derived fields exist anywhere else in the Add/Edit Product drawer: "Target stock levels (days)" is a toggle revealing plain per-channel numeric inputs (no computation; a static "3PL Warehouses Max: 30" default even when untouched is not a derived value); the Amazon/Walmart/Flexport Fulfillment/3PL sub-tabs are plain inputs aside from two pure mirrors (Amazon's readonly "Category" echoing the Product Details selection, and Walmart's "Sales Price" sharing List/Retail Price's Alpine variable) — neither does arithmetic.

- [2026-09-19T00:00:00.000Z] Cross-cutting test-coverage gap, all five Create/Edit suites (Suppliers, Freight Forwarders, Customs Brokers, Warehouses, Products): every section's Create/Edit spec asserts only a subset of the fields its own `buildFake*Data()` factory fills reflect on the resulting detail page/index row afterward — e.g. Suppliers Create never checks `address/city/phone/email/leadTimeDays/balanceTiming/licenseNumber` reflect anywhere (only `state+country`, `contactName`); Products Create's happy path never checks `listPrice`/`retailPrice` reflect anywhere despite setting both. The live audits above (this same date) manually filled that gap once and found it mostly clean aside from the real display-gap bugs above — but the automated suite still can't catch a regression in any of the never-asserted fields going forward. Worth widening each Create/Edit spec's assertions to cover every field its own fixture sets, not just adding new sections.

- [2026-09-20T00:00:00.000Z] **Verification of ClickUp IM-674's QA findings against a subsequently-deployed fix (PR #530), on request**: Joan Bonilla's comment on IM-674 (90180252018448, 2026-08-19) raised three clusters of findings on the Add Product drawer — re-tested live against the current app, all via fresh throwaway fixtures (not her original ones, which weren't available to re-open):
  - **"Target stock levels (days)": Maximum for AWD and Flexport not saving; 3PL should be 15 but displays 30 — CONFIRMED FIXED.** Enabled the custom-target-stock-levels toggle, set distinct Min/Max values for all five channels (Amazon/AWD/Walmart/3PL Warehouses/Flexport) including 3PL Max = 15, saved, and confirmed via both the read-only detail page and a freshly reopened Edit drawer that every single value (10 fields) round-trips exactly — AWD Max, Flexport Max, and 3PL Max=15 all correct, no reversion to a stale default.
  - **"Import and Duties": Duty/Tariff not saving in Fixed option; $/% symbol not updating on type change — CONFIRMED FIXED**, at least for the single-product (no parts) Import Duties fields (`duty_type`/`duty_value`, `tariff_type`/`tariff_value`). Setting both to Fixed ($15 duty, $8 tariff) and saving shows "Fixed: $15.00"/"Fixed: $8.00" and a correct Total ($23.00) on the detail page, and the same values reopen correctly in Edit. Separately confirmed the $/% suffix span (`x-text="dutyType === 'fix_rate' ? 'USD' : '%'"`) now updates live the instant the type dropdown changes, for both Duty and Tariff. Note: this is distinct from the still-open, narrower per-part "Duty Rate" **live-preview** glitch documented earlier in this file's 2026-09-19/20 Products entry — that one is unaffected by this fix (still shows $0.00 in the drawer while editing a Fixed-rate part, though it still saves correctly). Joan's original comment didn't mention "parts" at all, so it most likely referred to this now-fixed single-product flow.
  - **"Dropdown option in Shipping tab looks like a history" — NOT FIXED, still reproduces.** Specifications > Shipping's only dropdown, "Shipping Terms" (`<select>` with `class="kt-select"` but no `data-kt-select`/`data-kt-select-wrapper` — a plain native `<select>`, same class of gap as `category_id` documented above), still renders as the browser's own unstyled native option-list popup on click (plain white/blue-highlight OS chrome) instead of the app's custom KTUI dropdown styling used everywhere else (Country, Brand, Supplier, Category-in-filter, etc.) — visually exactly the kind of plain browser popup a non-technical tester would describe as "looks like a history." Needs converting to a KTUI select like its siblings.
  - **Not checked**: Joan's original screen recording and inline screenshots on that comment were not opened (no video playback available; images not downloaded) — this verification worked from her text description alone and re-created each symptom fresh rather than reproducing her exact original steps/data, so a very specific nuance unique to her original repro (if any) could theoretically have been missed. Confidence is otherwise high since each symptom was independently reproduced/disproved with a direct, deliberate test targeting the exact field named.

- [2026-09-21T00:00:00.000Z] **Suppliers blank-City crash (documented 2026-09-16/extended 2026-09-17 above) — CONFIRMED FIXED, found while verifying IM-663.** Re-tested the exact same repro on a fresh fixture (Edit drawer → clear `#city_edit` → change email → Save Changes): it no longer 500s with the raw `SQLSTATE[23502]` Laravel debug page. It now fails gracefully with a `.kt-alert-destructive` "Form submission failed. The city field is required." banner, staying on the edit page — proper server-side validation has been added. Not yet re-checked whether the equivalent Create-path repro (leaving City blank on Add Supplier) is also fixed; only the Edit/PUT path was re-verified.

- [2026-09-21T00:00:00.000Z] **Verification of ClickUp IM-663's QA findings (Service Providers > Suppliers Edit), on request**, re-tested live against the current app (bundled across PR #524/#534/#550, all closed/deployed to STAGING) via a fresh throwaway fixture:
  - **"Country Code (logo/image) is missing" next to Phone Number — CONFIRMED FIXED.** The Edit drawer's Phone Number field is now a `PhoneCallingField` component with a `.phone-calling-field__flag` KTUI select (`#cell_calling_country_id_edit`) showing each country's flag (`fi fi-{{flag}}`) and dialing code, matching the Figma design in the ticket's screenshot.
  - **"Operations field/Lead Time is missing" — CONFIRMED FIXED.** An "Operations" section with a "Lead Time to US (Days)" `input[type=number]` (`#shipping_speed_edit`) now renders in the Edit drawer.
  - **"Increase/Decrease buttons are missing" — CORRECTED to CONFIRMED FIXED.** Initially misjudged as missing from a wide, unfocused screenshot. Re-checked: `shipping_speed_edit` (`step=1`), `deposit_percentage_edit`/`balance_percentage_edit` (`step=0.01`) all have `min`/`max`/`step` set with computed `appearance: auto` (native spinner not suppressed) — a tight screenshot cropped to the field confirms the browser's native up/down spin buttons render at the input's right edge. The user caught this correction live (2026-09-21) by pointing out the `step` attribute; lesson: a wide full-drawer screenshot can miss a small native control that a zoomed/cropped shot on the field itself would catch.
  - **"Duplicate Payment Terms field in Edit Supplier" — CONFIRMED FIXED.** Only one "Payment Terms" section/heading renders now (was two, per the ticket's screenshot).
  - **"Editing/Adding brand or website url is not saving" — CONFIRMED FIXED (website URL; brand mapping not separately round-tripped).** Filled `#website_url_edit` with a test URL, saved, and confirmed the value persists on the resulting detail page. A `#brand_mapping_edit` KTUI `<select>` also exists in the form but its own save round-trip wasn't separately exercised here.
  - **Toast message remaining visible after reopening Edit drawer (Ivan's low-priority finding) — NOT REPRODUCIBLE.** Re-opening the Edit drawer for the same record right after a successful save shows no `.kt-alert-success` inside the drawer.
  - **Joan's later "I am still getting the error when editing a supplier" comment (video, 1789109685916) — CONFIRMED as a DISTINCT bug from the blank-City crash above, via a frame the user shared from that video.** It's a PHP `ParseError` (compile-time syntax error, not a runtime/SQL error) in `app/V2/Http/Actions/Supplier/Update.php:174` — "syntax error, unexpected token 'if', expecting 'function' or 'const'". The code shown (`$parts[] = 'Updated attached documents'; }` closing one brace too early, then `if (empty($parts))` landing at class-body scope) points to a botched edit adding an "attached documents" audit-changelog message. Since PHP can't compile a file with a syntax error, this would have 500'd **every** supplier PUT save app-wide, not just this record, for as long as the broken file was live — a more severe bug than the blank-City case even though it lives in the same file. Re-tested live against the exact same ticket test URL (`.../suppliers/WU9uY01DQ0NUcXFlbWZ2TWoxS1JJUT09/edit`) immediately after seeing the screenshot: saves cleanly now, no `ParseError` — already fixed by the time of this (2026-09-21) verification, most likely by one of PR #524/#534/#550.

- [2026-09-21T00:00:00.000Z] **Verification of ClickUp IM-643's QA findings (Customs Brokers > Create), on request**, re-tested live against the current app (PR #537, closed/deployed to STAGING) via fresh throwaway fixtures:
  - **Original ask ("make /v2/service-providers/customs-brokers/create work") — PARTIALLY fixed, one real bug remains.** The actual in-app flow (clicking "+ Add Customs Broker" on the index) opens a client-side drawer overlay and submits successfully with no navigation — confirmed working. But the literal URL `GET /v2/service-providers/customs-brokers/create`, when visited/refreshed/shared directly, throws an **unhandled 500**: `BadMethodCallException: Method App\V2\Http\Controllers\CustomsBrokerController::create does not exist.` The UI never navigates to this URL itself (so most users won't hit it), but it's a dead resourceful-route stub left over from before the drawer-based UI replaced a dedicated create-page — and since debug output is enabled, the error page also leaks a full stack trace and a raw SQL query (`select * from "users" where "id" = 1...`), which is an information-disclosure concern on top of the crash. Caught after this user flagged that direct-URL access specifically hadn't been tested — the initial pass only exercised the button-triggered drawer.
  - **UI: "Extension and Phone Number should be together" — CONFIRMED FIXED.** Telephone Number and a new "Ext" field now sit side-by-side in the same row in the Contact Information section.
  - **UI: "Email field should be beside Contact Name" — CONFIRMED FIXED.** Contact Name and Email now render as a two-column row.
  - **UI: "Missing 'Number' in the Routing label" — CONFIRMED FIXED.** The Bank Accounts section's field (shared with Suppliers/Freight Forwarders, see `BankAccountsSection.ts`) is labeled "Routing number", confirmed live in this drawer specifically.
  - **UI: "Attached files section is missing" — STILL NOT FIXED.** Walked the entire Create drawer's DOM (headings + labels): no attachment/file-upload section exists anywhere in it (Customs Broker Details / Address / Contact Information / Bank accounts / Additional Information are the only sections).
  - **UI: "All fields are missing the 'Enter' label inside the text fields" — STILL NOT FIXED.** Company Name, Street Address, City, State, ZIP Code, Contact Name, Email, and Customs Notes all still have empty `placeholder` attributes. Only Telephone/Cell Number (`"+1 5551234567"` format example) and Extension (`"Ext"`) have any placeholder text, and none read as an "Enter ___" hint.
  - **Functionality: "Extension number is not saving" — CONFIRMED FIXED.** Created a broker with Extension Number "4321": value round-trips correctly on the detail page's Contact Information card, in a fresh reopen of the Edit drawer, and in the index row.
  - **Functionality: "Extension number save not logged in Audit history" — CONFIRMED FIXED.** The detail page's "Audit Log" panel (not present/checked in earlier sessions' notes for this section) logs a new version per edit; editing only the Extension Number produced a "Version 2" entry explicitly reading "Updated Extension Number", beneath the original "Version 1 ... Customs Broker Created" entry.

- [2026-09-22T00:00:00.000Z] **IM-674's "V2 - Acceptance Tests" checklist (Products > Product Create), run live for the first time (all 4 items were previously unresolved on the ticket) — this is a new test category, distinct from this file's usual CRUD-suite verification: v1 (`/product/create`, top-nav shell) vs. v2 (`/v2/products` → Add Product drawer) field-parity plus Desktop/iPad Mini/iPhone 14 Pro Max responsiveness, all via throwaway `npx tsx` + Playwright device presets (`devices['iPad Mini']`/`devices['iPad Mini landscape']`/`devices['iPhone 14 Pro Max']`/`devices['iPhone 14 Pro Max landscape']`), no permanent spec written.**
  - **Compatibility with V1 — near-full parity, one real gap.** Every v1 field (Product Details, Target Stock Levels' 5 marketplaces × Min/Max, Specifications' Product/Packaging/Shipping tabs, Fulfillment/3PL's Amazon/Walmart US/Flexport tabs, Import Duties & Parts incl. the Yes-branch per-part table, Regulatory Requirements) has a live v2 equivalent, confirmed field-by-field by toggling every conditional branch (Target Stock Levels toggle, Import/Export Yes, Parts Yes + Add Parts) on both versions. v2 also adds a "Mixed" Duty/Tariff rate-type option beyond v1's Percentage/Fixed (additive, not a gap). **Gap**: v1's Specifications section has a page-level "Metric | Imperial" unit-switcher toggle affecting Product/Packaging/Shipping's dimension and weight fields; v2's Create Product drawer has **no equivalent anywhere** (confirmed 0 DOM matches for "metric"/"imperial", and visually absent next to the "Specifications" header) — units are hardcoded (cm/kg for Specifications, inches/Lbs for Fulfillment, which happens to match Amazon's own spec). Needs a product-owner call: intentional simplification or a dropped capability — not asserted as a bug outright. Two cosmetic-only relocations also noted: v1's "Product is discontinued" checkbox → v2's "Inactive" toggle button in the drawer header (same concept, moved); v1's "Choose File" input → v2's thumbnail + "Change" button (same capability, restyled).
  - **Responsiveness verdicts, per the user's 2026-09-22 severity call: Desktop and Mobile PASSED (placeholder-text truncation only, negligible); Tablet (iPad Mini) NOT PASSED (landscape orientation clips an entire input box, not just its placeholder text).** Placeholder-in-a-shrinking-textbox truncation is ordinary/negligible and doesn't fail a viewport on its own (no data loss, field stays fillable, save unaffected) — but Tablet fails specifically because iPad Mini landscape also clips the 3rd Package Dimensions box (Height, "inches") off entirely, a different mechanism (a control disappearing) than text shrinking inside a box that's still fully present. Confirmed via `document.documentElement.scrollWidth` (no page-level horizontal overflow anywhere — the drawer clips its own content, it doesn't widen the page) plus direct visual screenshots at 8 scroll depths per viewport:
    - **Desktop (1280×800, Chromium)**: Target Stock Levels' "Minimum"/"Maximum" number-input placeholders truncate to "Minimur"/"Maximu" (the up/down spinner eats width the placeholder needs); the Import Duties "Duty"/"Tariff" rate-type `<select>` truncates its selected value "Percentage" → "Percenta". Package Dimensions (Fulfillment/3PL tab, 3-across "inches" boxes) fits fine here.
    - **iPad Mini portrait (768×1024)**: Clean — the drawer goes full-width (single column) at this size, so every field gets enough room; "Minimum"/"Maximum" and all unit labels render in full, all the way to the Save/Cancel footer.
    - **iPad Mini landscape (1024×768)**: The worst-affected configuration tested. Same Target Stock Levels/"Percentage" truncation as Desktop, **plus** Package Dimensions' 3rd box (Height, "inches") gets clipped/cut off entirely rather than merely truncated — likely because at this width the drawer no longer gets the full-bleed single-column mobile layout (like its own portrait orientation) but also doesn't get Desktop's full target panel width, landing in a narrower in-between fixed-panel state worse than both neighbors. This means the Height field for Amazon package dimensions may be effectively invisible to a user who only ever opens this drawer on an iPad in landscape.
    - **iPhone 14 Pro Max portrait (430×740)**: Target Stock Levels truncation is most severe here — placeholders collapse to single letters ("Mi"/"Ma") across all 5 marketplace rows, because the drawer is full-width but the viewport itself is very narrow. Package Dimensions is unaffected here (renders all 3 "inches" boxes in full) — full-width single-column layout gives that particular row enough room even at 430px.
    - **iPhone 14 Pro Max landscape (814×380)**: Clean, same reason as iPad portrait — a full-width drawer at ≥~800px gives every field enough room.
    - No functional blocker found anywhere (every field remains fillable/submittable); the Desktop/iPad-landscape truncation is a cosmetic label-fit bug, but the iPad-landscape Package Dimensions clipping is a step above cosmetic since a field can become effectively hidden.
  - Not yet done: Products > Product **Edit** (a separate ticket, IM-618) was not touched by this pass. A static/possibly-outdated reference screenshot (see `reference-adxmanager-screenshots` memory) showed the Edit drawer's Target Stock Levels with "Walmart" duplicated and no "Flexport" row — spot-checked live on **Create** and it's fine there (all 5 marketplaces present, no duplicate), but Edit itself wasn't separately re-verified live, so that reference-image discrepancy remains unconfirmed either way.

- [2026-09-23T00:00:00.000Z] **IM-674 wrap-up: the two real (non-negligible) findings from the 2026-09-22 acceptance-test pass were relocated into QA Task Pool's existing consolidated trackers rather than left on IM-674 or filed as new standalone tickets, then IM-674 was moved to "approved for production" (not Closed).** This repo's QA Task Pool list (`1100780000006128`) already runs a small number of living, per-concern tracker tickets that different sessions/testers append rows to — worth checking there before opening any new ticket for a cross-cutting finding:
  - **IM-739 "Global Frontend: Responsive Layout Issues — iPad Mini & iPhone 14 Pro Max"** — a `Issue | Recommendation` table, one row per (device/orientation × affected modules), already covering Suppliers/Freight Forwarders/Customs Brokers/Warehouses/Dashboard. The Package Dimensions Height-box-clipped-on-iPad-landscape finding (Products > Product Create) was appended here as a new row rather than filed separately, since it's the same device/orientation category this ticket already tracks — Products just wasn't a module in it yet.
  - **IM-741 "Products (All Products + detail page): bugs & quirks found during review"** — a `# | Issue | Where | Steps to Reproduce | Expected Result | Actual Result | Priority` table, Products-scoped. The "Shipping tab dropdown looks like a history" finding (native `<select>` missing `data-kt-select`, same root cause as the already-documented `category_id` gap) was appended here as row 6, deliberately scoped to only what was actually re-verified live (the Shipping Terms field) rather than asserting the broader "site-wide dropdown" claim from the original 2026-09-20 comment, which was never independently confirmed beyond Products.
  - **IM-730 "Global Frontend QA: Shared Layout, Typography, UI Consistency and Responsiveness"** exists as the umbrella/global-issues ticket (explicitly says page-specific defects should be separate linked tickets) and **IM-735 "V2 QA Findings: Consolidated Tracker (All Modules)"** exists as a master per-module bullet-list index linking out to tickets like IM-739/IM-730 — neither was the right target for these two Products-specific findings, but both are useful to know about for future "where does this belong" triage.
  - Both IM-674 comments (the "V2 Acceptance Test" table and the older "IM-674 Verification — Fix Check Results" table) were edited in place to mark the moved rows "➡️ Moved to IM-739/IM-741" with a link, rather than leaving stale ⚠️/❌ entries with no forwarding pointer.
  - IM-674 itself was **not** closed: its Compatibility-with-V1 gap (missing Metric/Imperial toggle vs. v1) is still an open product decision pending Kevin Namuag's confirmation, so on explicit instruction the ticket's status was changed to `approved for production` instead (a valid status on this list, also used by the ClickBot PR-deployment comments) — closing was deliberately deferred until that one question resolves.

- [2026-09-22T00:00:00.000Z] **IM-598's "V2 - Acceptance Tests" checklist (Products > Brands), run live for the first time — v1 (`/brand`) vs. v2 (`/v2/brands`) field-parity plus Desktop/iPad Mini/iPhone 14 Pro Max responsiveness, all via throwaway `npx tsx` + Playwright (device presets + a hand-built viewport for Desktop), no permanent spec written.**
  - **Both of IM-598's previously-reported findings are CONFIRMED FIXED live**: the index table's trailing action column now renders a blank header (not the text "Actions") with the per-row kebab menu intact (PR #524); the breadcrumb reads "Products > Brands" (PR #537), not "Catalog > Brands".
  - **Compatibility with V1 — one real, confirmed-live functional regression in the Create Brand form's Batch Number "Type" field.** v1's `/brand` "Add Brand" page constrains Batch Number "Type" to a `<select>` of exactly 6 enumerated generator schemes ("Number (increasing/random)", "Alphabet (increasing/random)", "Alphabet Numeric (random)", "Date Based") and shows a live "Example: <prefix><generated><suffix>" preview next to it as Prefix/Type/Suffix are filled in. v2's "Create Brand" modal renders the same field as a **plain unconstrained free-text `<input>`** (confirmed: no `role`/`aria-haspopup`, accepts arbitrary keystrokes) with **no live example preview at all**. Verified all the way through a real save (not just the in-form state, per this file's standing round-trip discipline): submitted Prefix "ZZ" / Type "bogus-not-a-real-type" / Suffix "01" → got a 302 success redirect, "brand created successfully" banner, and the detail page's Brand Information card displays "Batch Number ZZ / bogus-not-a-real-type / 01" verbatim, i.e. the invalid string is genuinely persisted and rendered, not rejected client- or server-side. Since Batch Number Type presumably drives actual batch-number generation logic elsewhere (Inventory), an arbitrary string here is a real data-integrity gap, not a cosmetic one — flagged for follow-up, not asserted as fixed. All other Create Brand fields (Brand Name, Corporations multi-select — checkboxes in v2 vs. a searchable multi-select dropdown in v1, functionally equivalent — Batch Prefix/Suffix, Brand Logo upload) have full v1/v2 parity.
  - **Index row Active/Inactive indicator — PASSED, correcting this file's own initial write-up.** v2's Brands index doesn't copy v1's colored-strip-per-row styling, but it doesn't need to: each row's kebab (`⋮`) menu is itself contextual on status — confirmed live that an active brand (Cossini) offers "View"/"Deactivate" while an inactive one (DuraFlask) offers "View"/"Activate", so the current status is always inferable from which action the menu offers, one click away, no separate visual indicator needed. User caught that the first pass over-flagged this without actually opening the menu.
  - **Responsiveness: PASSED at Desktop, iPad Mini (both orientations), and iPhone 14 Pro Max landscape for the Brands index + Create Brand modal — no truncation, no clipped controls.** iPhone 14 Pro Max **portrait** initially looked like the Brands index was missing its Corporations and kebab-action columns entirely (page-level `document.documentElement.scrollWidth` check is a false negative here) — traced to a `kt-scrollable-x-auto` wrapper div around just the `<table>` with its own internal `overflow-x: auto`; scrolling that inner container right reveals Corporations and the kebab menu fully intact. This is a legitimate horizontally-scrollable-table pattern (content is reachable, nothing is actually lost), not the "control clipped off" failure mode this file's acceptance-test discipline reserves for a hard fail — marked PASSED, but noting the interaction (horizontal scroll needed) for the record since it's not obvious without trying it.
  - **Responsiveness: iPad Mini LANDSCAPE FAILS on the Brand detail/view page specifically (not caught in the first pass, which only checked the index + Create modal) — same "worst-affected in-between width" pattern already logged for IM-674's Product Create drawer.** At 1024×768, the header row's Deactivate/Edit/Delete button group and the "Audit Log" side panel don't wrap onto separate lines the way they do at both neighboring viewports (Desktop 1280px keeps them as two clearly-separated regions with a real gutter; iPad Mini **portrait** 768px stacks Audit Log below Brand Information as its own full-width section) — at 1024px landscape they're forced into the same row with almost no gap, so the red "Delete" button sits immediately flush against the "Audit Log" panel's left edge/header text, a real visual-collision risk (worse if a future rename made the button label longer). The Brand Information card's "Marketplace" field label also truncates to "Marketplac" at this same width from the same horizontal squeeze. Confirmed via direct screenshot comparison across Desktop/iPad-portrait/iPad-landscape on the same brand (Cossini); not asserted as blocking (nothing is unclickable) but flagged as a genuine layout bug, not cosmetic truncation, per this file's distinction between shrinking placeholder text (negligible) and a control/panel actually colliding with its neighbor.
  - QA fixture cleanup: the "QA Batch Type Test ..." brand created to reproduce the Batch Number Type bug was deleted afterward via the v2 detail page's Delete button; confirmed removed from the index before ending the session.

- [2026-09-22T00:00:00.000Z] **IM-602's "V2 - Acceptance Tests" checklist (Service Providers > Customs Brokers Index), run live for the first time — v1 (`/customsbroker`) vs. v2 (`/v2/service-providers/customs-brokers`) field-parity plus Desktop/iPad Mini/iPhone 14 Pro Max responsiveness, all via throwaway `npx tsx` + Playwright device presets, no permanent spec written. Builds on this same ticket's earlier plain QA-comment verification (checkbox/filter/kebab findings) done the same day.**
  - **Compatibility with V1 — one real, confirmed-live functional regression: v2 dropped the hard "Delete" action entirely.** v1's row action menu (`.dropdown-menu`, hamburger icon) has three separate actions: **Edit** (real `<a href=".../customsbroker/{id}/edit">` dedicated page), **Delete** (a real `method: delete` form submission — hard delete), and **Inactive** (a separate soft status toggle, `actionStatus` link). v2's kebab menu has only **Deactivate** — the same soft-toggle concept as v1's "Inactive" — with no Delete option anywhere and Edit relocated one click away behind the detail page's own Edit button (confirmed present and functional). So Edit is a workflow change (2 clicks instead of 1), not a capability loss, but the hard-Delete capability itself is simply gone in v2. Matches this repo's own `CustomsBrokersPage.ts` comment ("Customs Brokers have no hard Delete at all") — now confirmed to be a genuine v1→v2 drop, not just a UI-mechanic difference. Needs a product-owner call on whether soft-deactivate-only is the intended end state (common modern pattern) or a dropped capability — not asserted as a bug outright, same treatment as IM-598's Batch Number Type gap.
  - **Checkbox column ("missing per Figma", QA-flagged 2026-05-13) — NOT a v1 parity gap.** Checked v1's `/customsbroker` table directly: it has no row/header checkboxes either (the blank-looking first header cell is actually a colored active/inactive status stripe on the row's left edge, not a checkbox column). So the missing checkboxes are a net-new Figma design element being requested, not a regression from v1 — refines this ticket's earlier finding (still not implemented in v2, but "missing vs. Figma" is the right frame, not "missing vs. v1").
  - **Filter mechanism — different but equivalent interaction, not a gap.** v1 uses a small "Add Filters" popover (funnel icon next to the page title) with a single "Status" `<select>`; v2 uses a black "Filter"/"Filter Applied" button that opens a slide-out drawer with Active/Inactive checkboxes. Different UI, same capability (filter the list by status) — consistent with this repo's own prior note that Customs Brokers' drawer-based filter already differs intentionally from Suppliers' dropdown-based one.
  - **Responsiveness: PASSED at Desktop (1280×800) and both iPad Mini orientations for the Customs Brokers index** — the table only ever needs its own internal `.kt-scrollable-x-auto` horizontal scroll to reach the later columns (Ext/Cell Number/Email/kebab), same legitimate scrollable-table pattern already established for Brands; manually scrolled the wrapper on iPad Mini portrait and confirmed every column (through Email) is intact and reachable, nothing clipped or lost.
  - **Responsiveness: iPhone 14 Pro Max PORTRAIT has a genuine page-level horizontal scroll (`document.documentElement.scrollWidth` 461px vs. a 430px viewport) — but it's a site-wide global-header bug, not specific to this page.** Traced the exact offending element via computed styles: the app's fixed top `<header class="kt-header ...">` bar itself renders 461px wide against the 430px viewport (confirmed by checking which unclipped ancestor's own `getBoundingClientRect().right` matches the measured page overflow, as opposed to the Customs Brokers `<table>`, which also extends far past the viewport at this width but is correctly contained by its own scroll wrapper and does NOT contribute to the document-level overflow). Since the header renders identically across every v2 page, this 31px overflow is very likely reproducible site-wide at this exact viewport, not something introduced by the Customs Brokers Index specifically — flagged for a frontend dev to check the header's own responsive CSS, not filed as a Customs-Brokers-page bug. iPhone 14 Pro Max landscape (814×380) has no such overflow.

- [2026-09-22T00:00:00.000Z] **Site-wide link crawl, requested ad hoc (not a permanent spec) — one throwaway `npx tsx` BFS crawler seeded from a single `/v2/home` page load (confirmed live: every sidebar link, including collapsed accordions, is already present in the DOM unhidden, so the full top-level nav map — 43 pages — is readable without clicking each accordion open).** Crawl followed every same-origin `<a href>` reachable from there (one sample per dynamic-ID URL pattern, e.g. one product detail page rather than all ~30), explicitly skipping known state-mutating GET links (`toggle-status`, `deactivate`, `/activate`, `/delete`, `logout`) so it stayed read-only. User stopped the run partway through (70 of an estimated 120+ reachable page-patterns actually visited, roughly a dozen top-level sidebar sections — Categories, Customs Brokers, Warehouses, Warehouses Inventory, most Reports beyond Sales/Sales Transactions, Tools beyond Order Planner, Integrations, Notifications — never reached before stopping), so this is a **partial** result, not a full-site clearance.

  - **Confirmed broken link: sidebar "Adjustments" (Reports section) → `/adjustments` 404s.** Plain HTTP 404, unambiguous, reached directly from the `/v2/home` sidebar link with no redirect.
  - **Confirmed broken link: sidebar "Payment History" (Accounts section) → `/v2/accounts/history` throws an unhandled 500.** Also reached directly from the sidebar with no redirect; not yet inspected for a stack trace/root cause, just confirmed live as a 500.
  - **Script limitation, not a confirmed bug**: 6 links that trigger a file download (order/shipment invoice PDFs, various `.../export` CSV endpoints on Inventory/Order Planner/Error Log) can't be checked via a normal Playwright `page.goto` — the browser treats the response as a download and the navigation call rejects, logging as an error even though the link itself may well work fine. These need to be re-tested with a plain authenticated HTTP request (reusing the session's cookies) instead of a full page navigation before concluding anything about them either way.
  - **Likely false positive, not confirmed**: both Error Log pages (`/v2/settings/error-log` and legacy `/errorlog`) got flagged by the crawler's crash-signature text scanner (`/exception|stack trace/i` in the page body) — almost certainly because an error-log *viewer* legitimately displays those words as literal log content, not because the page itself crashed. Both returned a normal 200. Worth a two-second eyeball check before ruling either way, but not logged here as a real defect.
  - Reconfirmed the already-documented "Sales report is slow" finding on both routes: v2 `/v2/reports/sales` took ~29.5s and legacy `/sales` took ~37.3s for a plain page load in this run.
  - The crawl also surfaced that a large share of "detail page" action links (Order Copy/Edit, Shipment Edit, Product Copy/Edit, and each section's own index) route through the **legacy non-`/v2/` URLs** (`/order`, `/shipment/list`, `/product`, `/brand`, `/category`, `/supplier`, `/users`, `/templates`, `/tenants`, `/fbafee`, etc.) rather than a v2 equivalent — all of those loaded fine (200) in this run, just noting the v1 surface is still very much live and reachable from v2 pages, not fully replaced.

- [2026-09-22T00:00:00.000Z] **Second site-wide link crawl, a separate ADX Manager deployment at `http://<cloned-staging-host>`, requested ad hoc (not a permanent spec).** Confirmed live to be the same "ADX Manager" application (same login flow, same route names) but a distinct server from `adxmanager.dev` (different IP entirely — `adxmanager.dev` resolves to `35.75.59.174`/`57.181.5.219`), served over plain HTTP with an older Bootstrap-themed UI on its v1 pages. Auth for this crawl used a session cookie the user copied manually out of their own already-logged-in browser (`adx_manager_session` + `XSRF-TOKEN`), passed to the crawler via env vars only, never written to a file on disk — Claude Code's own auto-mode classifier twice blocked more direct approaches (a scripted login POST to a raw IP, then a version of the crawler with the cookie value hardcoded into a script file) as credential-leakage-shaped, both correctly.

  - **Crawler methodology bug worth recording for any future re-crawl of this site (or adxmanager.dev)**: the first pass's dynamic-ID detection (any single path segment ≥10 characters treated as a hashed record ID and collapsed to a generic `:id` pattern for dedup purposes) was too broad — it also matched plain multi-word English slugs with no digits at all (`freightforwarder`, `customsbroker`, `sales-overview`, `inventory-health-report`, etc.), silently skipping **26 of this instance's ~44 top-level sidebar pages** because they all collapsed onto the same pattern already claimed by `/all-inventory` (the first long slug visited). Fixed by requiring the segment to actually contain a digit (or be a bare date/integer) before treating it as a dynamic ID — real hashed IDs on this app are always mixed alnum with digits; real slugs never are. A follow-up run with the fix confirmed all 26 previously-skipped pages (Ready to Ship, Inventory Warehouses, Freight Forwarders, Customs Brokers, Warehouses, Sales Overview, Report Payment, Warehouse Cost, Stock Transfer, Inventory Value, Adjustment, FBA Receiving, Inventory Health Report, Customer Return Report, Customer Shipment Replacement Report, Order Planner, Shipment Planner, SKU Converter, Carton Resizer, Product Disposal, Export Data, Product Identifiers, Corporation, Integrations, Notifications, User Settings) return 200 with no crashes, plus their own Create/Edit/Copy sub-pages.
  - **Confirmed broken link, same class as the adxmanager.dev crawl's own confirmed bugs**: `/billing` 404s (reached directly from the `/home` sidebar, no redirect) — reproduced identically across both crawl passes on this instance.
  - **Confirmed broken link: `/v2/accounts/history` throws an unhandled 500** — same page/bug as already documented on adxmanager.dev proper, now confirmed present on this deployment too. Got an actual stack trace this time (adxmanager.dev's own crawl only saw the status code): `ErrorException` in `resources/views/v2/accounts/history.blade.php:91` — `Attempt to read property "full_name" on null` (Laravel 13.32.0 / PHP 8.3.6). Root cause: the view assumes a related user/record is always present and dereferences `->full_name` without a null check.
  - **Likely false positive, same as adxmanager.dev**: both `/errorlog` and `/v2/settings/error-log` get flagged by the crash-signature text scanner purely because they're log *viewers* that legitimately display words like "exception" as literal log content — both are plain 200s, not actual crashes.
  - **Same "script can't validate download-triggering links via a normal page navigation" limitation as adxmanager.dev**, now hitting 9 links on this instance: `/order/customer-invoice/...`, `/order/censored-invoice/...`, `/order/purchase-order/...`, `/shipment/invoice-generator/...`, `/shipment/censored-invoice/...`, `/shipment/packing-invoice-generator/...`, plus the `/export`-suffixed CSV endpoints on Inventory/Order Planner/Error Log. All of these need a plain authenticated HTTP request instead of a browser navigation to actually check.
  - Minor, non-blocking client-side JS errors observed via the page-health monitor (page still rendered/functioned, so not counted as page failures): `/order/create` and `/order/{id}/copy` throw "Invalid dropzone element." in the console; `/category/create` and `/category/{id}/edit` throw "ClassicEditor is not defined." Both look like a missing/misconfigured third-party JS widget init rather than a functional break — worth a frontend look but not asserted as broken pages here.
  - No other 4xx/5xx or crash-signature matches found across either pass on this instance.

- [2026-09-22T00:00:00.000Z] **Second site-wide link crawl, different host: `http://<cloned-staging-host>` (an IP-addressed deployment, reached via `/system-login` → `/home` → manual `/v2/home`, distinct from the `https://adxmanager.dev` domain used everywhere else in this file — likely a staging/UAT box running the same v2 codebase, not verified which). A fresh throwaway `npx tsx` BFS crawler (60 internal pages visited, 1203 unique links discovered, 104 external links all healthy) — full run this time, not partial.**

  - **⚠️ Crawler mistake, flagged for anyone writing a future link-crawl script here: this run's status-check phase used a plain authenticated GET against every discovered link indiscriminately, including `.../toggle-status` action links rendered as bare `<a href>` elements in table rows (Products, Categories, Brands, Service Providers' Suppliers/Freight Forwarders/Customs Brokers/Warehouses, Shipments, Settings > Corporations). Because the app performs the toggle on a plain GET (no POST/CSRF gate), the crawl itself flipped the active/inactive status of ~115 real records** (25 Products, 25 Customs Brokers, 25 Warehouses, 25 Shipments, 5 Categories, 7 Corporations, 1 each of Brands/Suppliers/Freight Forwarders) **as a side effect of merely checking whether the links were "broken."** The *earlier* adxmanager.dev crawl earlier this same day (entry above) explicitly excluded `toggle-status`/`deactivate`/`/activate`/`/delete`/`logout` link patterns from its GET-checks for exactly this reason — this run's script was written fresh without re-checking that precedent first. **Any future link-crawl script against this app must skip GET-checking those same patterns** (and treat their discovery as "found, not checked" rather than silently omitting them). Since the toggle is a simple boolean flip with no known confirmation step, re-firing the same 115 GET requests should restore the original state (assuming nothing else touched those records in the interim, and assuming the toggle has no side effects beyond the flag itself, e.g. notifications) — not done automatically, pending the user's confirmation.
  - **Confirmed broken link (cross-environment corroboration): sidebar "Adjustments" → `/adjustments` 404s, and sidebar "Payment History" → `/v2/accounts/history` throws an unhandled 500 — both reproduce identically on this host, same as adxmanager.dev above.** Both are global-sidebar links (present on all 44 crawled v2-shell pages), so this hits every v2 page's nav, not a one-off.
  - **New confirmed broken link, this host only (not checked against adxmanager.dev): legacy sidebar "Billing" → `/billing` 404s** (found on 9 legacy-shell pages, e.g. `/order`, `/shipment/list`).
  - **New confirmed bug: the Shipments list (`/v2/shipments`) renders each row's shipment reference (order #, STAR-xxxx, FBA-xxxx, WFB-suffixed IDs) as an `<a href="{id}">` with no path prefix at all** — e.g. `<a href="5353">5353</a>`. Resolved relative to `/v2/shipments`, the browser lands on `/v2/5353` (replacing the last path segment, standard relative-URL resolution), which 404s. All 23 sampled row-reference links on this page have the same malformed-href pattern; this is a page-wide bug affecting every single shipment row's primary reference link, not an isolated record.
  - **New confirmed bug, record-specific: one shipment (`bCsrVE9zSlRHSmZFU0dWZ3duWkY2Zz09`) 500s on every one of its own document-generation endpoints** — invoice, censored-invoice, and packing-list, both via the `/v2/shipments/{id}/documents/...` route and the legacy `/shipment/{invoice-generator,censored-invoice,packing-invoice-generator}/...` route (6 broken URLs total, all for this one record). Dozens of sibling shipments' identical-pattern invoice links all return 200 — this looks like malformed/missing data on that one shipment (e.g. a missing address or line-item field the PDF template needs) rather than a route-level bug.
  - **New confirmed anomaly, not yet root-caused: `/v2/products/56` (via `apiContext.get`, not a full page render) redirects to `/home`** instead of either the product detail page or a 404 — worth a follow-up to see whether product 56 is in some invalid/edge-case state (e.g. soft-deleted, wrong tenant) that the controller redirects away from rather than erroring on.
  - **Two navigation timeouts reproduced on both this host and adxmanager.dev**: `/v2/inventory-warehouses` ("Warehouses Inventory") and `/v2/reports/sales`/`/sales` ("Sales") both fail to reach Playwright's `networkidle` within 20s on this host too, consistent with the already-documented "Sales report is slow" finding above — likely a shared perf issue in the underlying report/warehouse-inventory query, reproducible across environments rather than one flaky box. `/all-inventory` (legacy) also timed out here; not previously checked on adxmanager.dev.
  - **Re-confirmed script limitation (not a bug): 2 links legitimately trigger a file download** (`/v2/dashboard/export/csv...`, `/v2/inventory/all/export...`) and get misreported as "broken" by a full-page-navigation crawler for the same reason already documented in the adxmanager.dev entry above — Playwright's `page.goto` rejects when the response is a download, not a page.
  - External links (104 checked: Amazon Seller Central product/order links, Walmart product links, a ThemeForest author-page link) were all healthy — the ones that "redirect" do so to an expected login/bot-check page (Amazon `/ap/signin`, Walmart `/blocked`), not evidence of anything broken in this app.

- [2026-09-23T00:00:00.000Z] **IM-665's "V2 - Acceptance Tests" checklist (Service Providers > Freight Forwarders Edit), run live for the first time (all 4 items previously unresolved) — v1 (`/freightforwarder/{id}/edit`) vs. v2 (`/v2/service-providers/freight-forwarders/{id}/edit` → Edit Freight Forwarder drawer) field-parity plus Desktop/iPad Mini/iPhone 14 Pro Max responsiveness, via throwaway `npx tsx` + Playwright device presets, no permanent spec written. Builds on this same ticket's earlier plain QA-comment verification (Brand/Tel-Ext/Fax/validation-styling findings) done 2026-09-21.**
  - **Compatibility with V1 — one real, previously-unflagged functional regression: v2 dropped v1's "Make Default" checkbox entirely.** v1's Edit form (`company_name, address, city, country, state, zip_code, contact_name, tel_number, extension_number, cell_number, email_address, fax, sp_service, ltl_service, make_default`) has a "Make Default:" checkbox as its last field; v2's Edit drawer has no equivalent control anywhere. Not previously reported by QA — a genuine v1→v2 capability drop, needs a product-owner call on whether it was intentionally dropped.
  - **Reframes 2026-09-21's own "Brand is missing" finding: confirmed NOT a v1 parity gap.** v1's Freight Forwarder Edit form has no Brand field either (same reframing already applied to Customs Brokers' missing checkbox column, IM-602) — it's a net-new Figma/design ask, not a regression.
  - **Context for 2026-09-21's other two open findings**: v1 already keeps Telephone Number and Ext as two separate fields (stacked, not merged) — so Angelo's "keep separate, place side-by-side" resolution is at least consistent with v1's own field model, even though v1 itself doesn't put them in the same row. v1 also still has a "fax" field — so Joan's original ask to remove Fax would be dropping a capability v1 already has, not fixing a v1→v2 regression; worth this nuance when that finding gets triaged.
  - **v2-only additions, not gaps**: Bank accounts section and Portal Users section exist in v2 with no v1 equivalent — confirmed intentional feature adds.
  - **Required-field strictness increased in v2**: v1's Edit form marks only "Company" as required; v2 marks 8 fields required (Company Name, Street Address, City, State, Country, ZIP Code, Contact Name, Telephone Number, Email) — a real behavior tightening worth knowing about, not asserted as a bug.
  - **Responsiveness: Desktop, both iPad Mini orientations, and iPhone 14 Pro Max landscape all PASSED** — no clipped controls; iPad Mini landscape shows the Company Name *value* visually truncating inside its own input box (e.g. "QA Automation Kihn - Leuschk...") at the narrower in-between width, same negligible input-text-overflow behavious as any single-line text box, not a clipped control.
  - **Responsiveness: iPhone 14 Pro Max PORTRAIT NOT PASSED — a real, drawer-specific bug distinct from the already-documented Customs Brokers header overflow.** Confirmed the plain Freight Forwarders index page has zero overflow at this viewport (`header` width = exactly 430px, matching the viewport). Opening the Edit Freight Forwarder drawer specifically makes both the drawer (`#edit-freight-forwarder-drawer`, computed `max-width: 90%` resolving against a 574px basis instead of the 430px viewport) and the global `#header` balloon out to 574px wide — a 144px overflow, reachable only via horizontal scroll. Effect: the "Save Changes" button is pushed almost entirely off-screen (only "Cancel" fully visible) and the right portion of every input field (State, ZIP Code, Ext, Fax, etc.) is cut off. No inline JS-set width found on the drawer (`style="z-index: 100;"` only) — looks like a pure CSS `max-width: 90%` cascade issue specific to this drawer's open state at this exact viewport, not a JS bug.

- [2026-09-23T00:00:00.000Z] **IM-665 redo on request, same v2-acceptance-test pass re-run against the separate IP-hosted deployment (`http://<cloned-staging-host>`, see the 2026-09-22 entries above for what this host is) instead of `adxmanager.dev` — env var `ADX_BASE_URL=http://<cloned-staging-host>` passed at the shell level only (never written to `.env` or any committed file), so `dotenv.config()`'s default no-override behavior left it in place.** v1 record IDs are identical between the two hosts (e.g. Fedex's freight-forwarder hash is the same on both), confirming this host shares the same underlying database — so this redo is really testing for **frontend/build differences between the two environments**, not data differences.
  - **Key delta from the 2026-09-23 adxmanager.dev run: this host's v2 Edit Freight Forwarder drawer HAS a "Make Default" control — adxmanager.dev's did not (as of the same day).** A new "Preferences" section with a "Make Default" toggle switch and helper text ("When enabled, this forwarder will be used by default for new shipments.") is present here, giving genuine v1 parity for that field. This means the "v2 dropped v1's Make Default checkbox" finding logged above for adxmanager.dev **does not reproduce on this host** — the two environments are running different frontend builds at this exact point in time, this one further ahead. Re-check adxmanager.dev before treating that finding as still-open there.
  - **Everything else matches the adxmanager.dev run exactly, confirmed independently on this host**: no Brand field anywhere in the Edit drawer (0 matches, same reframe as before — still not a v1 parity gap since v1 doesn't have it either); Telephone Number/Ext still two separate fields side-by-side; Fax field still present; no file-upload input; the "validation error at the back and blurry" fix holds (sharp, opaque, fully-visible banner + inline message on a blank required field).
  - **The iPhone 14 Pro Max portrait drawer/header-overflow bug reproduces identically on this host** (430px viewport → 576px `scrollWidth` after opening the Edit drawer, vs. 574px on adxmanager.dev — same mechanism, trivial ~2px difference likely just from this host's drawer having one extra "Preferences" section). Confirms this is a shared frontend bug present in both environments' current builds, not host-specific. Additional nuance found this pass: the drawer's duplicate desktop/mobile "Save Changes" button pair (already documented elsewhere in this file as a standing pattern) means the **top sticky-header copy is the one that gets clipped off-screen, but a second, in-flow copy at the bottom of the form becomes reachable after scrolling** (whether via manual horizontal swipe or Playwright's `scrollIntoViewIfNeeded` dragging the viewport rightward) — so this bug does not fully block saving on a real device, but still forces a non-obvious horizontal scroll to reach either the top Save Changes button or several fields' right portions, which is still a genuine NOT PASSED per this ticket's own clipped-control-vs-cosmetic-truncation distinction.
  - Desktop, both iPad Mini orientations, and iPhone 14 Pro Max landscape all re-confirmed PASSED on this host too, same as adxmanager.dev.

- [2026-09-23T00:00:00.000Z] **IM-665 wrap-up, same pattern as IM-674's 2026-09-23 entry above: checked QA Task Pool first for an existing match before filing, found none (only a related-but-distinct row already on IM-739 about the plain index page's search bar, not the Edit drawer), then appended the Edit-drawer iPhone-portrait overflow bug to IM-739 as a new row scoped only to Freight Forwarders (Edit) — not asserted as reproducing on Suppliers/Customs Brokers/Warehouses' own Edit drawers since those weren't independently tested this pass. IM-665 was then moved to `approved for production` on explicit instruction, same as IM-674 — the "Make Default" v1-parity item is resolved on the newer `<cloned-staging-host>` build but still worth a follow-up check against adxmanager.dev before assuming it's live everywhere.**

- [2026-09-23T00:00:00.000Z] **Site-wide v2 responsiveness sweep for IM-739 (all 38 `/v2/` sidebar pages + 14 sampled detail pages + 5 Edit/2 Create URLs + every Add/Edit/Filter drawer + 24 filter-applied states), iPad Mini + iPhone 14 Pro Max presets, both orientations, via throwaway `npx tsx` (deleted afterward), read-only (drawers opened, never submitted; no toggle/delete links touched).**
  - **Measurement pitfall worth remembering**: with `isMobile` device presets, `window.innerWidth`/`clientWidth` GROW to match over-wide content (layout viewport zooms out), so comparing `scrollWidth` against `innerWidth` hides real overflow. Always compare against the preset's own `viewport.width` (injected as a constant) instead.
  - **Root cause of IM-739 row 8 (iPhone portrait Edit-drawer overflow) found**: the header breadcrumb's last crumb (`span/a.shrink-0` with the record name) never truncates, so a long record name widens `#header` and the whole page past 430px; drawers then anchor to that widened edge. Reproduced on Customs Brokers, Products, Orders, Shipments, Suppliers detail pages. Orders/Shipments detail also have the non-wrapping "Create Test Deposit/Customs Payment" buttons + unwrapped tables contributing.
  - **New iPad Mini landscape bug**: list pages whose table is NOT inside `.kt-scrollable-x-auto` (Sales Transactions, Order Payments, Export Sales, Warehousing Costs, Stock Transfers, Corporations, Integrations, Error Logs, Orders detail) widen the page to 1350–1930px; header breadcrumb/avatar go off-screen and Filter/Create drawers open fully or partly off-screen (only the blurred backdrop visible).
  - Row 4 (iPhone search overflow) no longer reproduces; rows 1/2/3/5/6/7 still reproduce and extend to more modules. Sales (`/v2/reports/sales`) timed out at 60s on 3 of 4 devices (known perf issue), Shipment Planner returned a one-off 502 on iPad portrait.
- [2026-09-23T00:00:00.000Z] **Verification of ClickUp IM-740 (filter validation / unhandled 500 on bogus filter values), on request**, re-tested live against adxmanager.dev after PR #555 (closed/deployed to STAGING) via a throwaway `request.get` probe across all 24 `FILTER_PAGES` (read-only list GETs, no mutating links). Result: the original fix works — all 13 originally-affected pages now return 200 for `__bogus_value__` and a SQL-ish `1' OR '1'='1` on their own filter param, and the bogus value is **ignored** (full unfiltered list, e.g. Suppliers "Showing 25 of 55", Products "25 of 118"), not 0 results. Valid filters still work on all 24. **Not fixed:** Kevin's follow-up (`filters[product_id][]=XXXXXX`) still 500s with the full Laravel debug page (`SQLSTATE[22P02] invalid input syntax for type bigint`, RDS host exposed) on **Orders, Shipments, Ready To Ship, Sales Transactions** — the 13 original pages don't use `product_id` and just ignore it; `product_id=-1` is fine everywhere. Gotcha for future probes: Error Logs pages naturally contain "Stack trace"/`vendor/` text in their row content, so a regex leak-detector false-positives there whenever a filter is ignored — check HTTP status, not body text, for that page.
- [2026-09-23T00:00:00.000Z] **Site-wide V2 dropdown sweep (follow-up to IM-741 row #6 "Shipping tab dropdown looks like a history"), ad hoc throwaway crawler, read-only (mutating links skipped).** Checked for an existing ticket first: none on QA Task Pool or V2 - Dev list (901818670324); the only record is IM-741 row #6 (moved there from IM-674's V2 Acceptance Test comment). Crawled 85 V2 page patterns (sidebar + one sample per detail/edit pattern; 42 shipment document links were file downloads, skipped). Classified every `<select>`: **KTUI** = `data-kt-select` + sibling `[data-kt-select-wrapper]` (235 instances); **native w/ `kt-select` class only** (styled closed state but opens the OS native list, 256 instances); **fully unstyled native** (`kt-input` class, no kt-select at all, 96 instances: Orders detail Edit Item "Seller SKU", Warehousing Costs Add form Product/Supplier/Storage Type, Corporations Edit "State"). All 24 Filter drawers are 100% KTUI; the native ones live in create/edit forms, Tools pages, Dashboard chart controls, Reports month/year pickers, and the global User Settings "Date Format". Gotcha: Sales Transactions takes >45s to reach networkidle; use domcontentloaded + wait.
- [2026-09-24T00:00:00.000Z] **Verification of ClickUp IM-772 (the V2-board copy of IM-739, "Global Frontend: Responsive Layout Issues — iPad Mini & iPhone 14 Pro Max") after PR #560 (closed/deployed to STAGING), on request** — re-tested live on adxmanager.dev via throwaway `npx tsx` scripts (deleted afterward) using the exact `devices['iPad Mini' | 'iPad Mini landscape' | 'iPhone 14 Pro Max']` presets; read-only (drawers opened, never submitted; no toggle/delete links touched). The PR repo (`AdxDave/adx-inventory`) isn't reachable via `gh` from this machine — used the ClickBot comment's PR summary instead.
  - **Fixed (rows 1–8):** iPad portrait "Filter Applied"/Download CSV stay on the search row on all 4 Service Providers index pages (FF too, though the PR only listed Suppliers/CB/Warehouses); iPad landscape detail pages (Suppliers/FF/CB/Warehouses/Brands) now put Audit Log in a full-width card below the content (`col-span-full xl:col-span-3`) and the action-button row never touches the title (16px gaps); iPhone portrait search bar contained; Dashboard `#sidebar_toggle` collapses 280→80px with `#content` following; Warehouses Add *and* Edit Location both open `#warehouse-location-drawer` (`kt-drawer-end`) in both iPad orientations; Products Add/Edit drawer Package Dimensions L/W/H (Amazon `pkg_depth/pkg_width/pkg_height` — note Length is `pkg_depth`, not `pkg_length`) and Flexport (`flexport_package_*`, behind a `button` tab labelled "Flexport") all ~100px wide inside the 410px drawer; Edit drawers on Suppliers/FF/CB/Warehouses/Brands contained at 430px (header breadcrumb's last crumb is now `min-w-0 truncate`).
  - **Not fixed (row 9):** Sales Transactions, Order Payments, Export Sales, Warehousing Costs, Stock Transfers, Corporations, Integrations, Error Logs still widen to 1356–1932px at iPad landscape; Filter/Create drawers open partly or fully off-screen (Sales Transactions/Error Logs Filter drawers start at x=1032/1139, i.e. only the backdrop shows). Root cause traced: tables ARE now inside `.kt-scrollable-x-auto`, but that wrapper sits inside `div.hidden.lg:block`, a grid item of `div.grid` with default `min-width:auto`, so the grid item grows to the table's width and the scroller never scrolls. Suppliers (working) has `kt-scrollable-x-auto` on the grid item itself (a scroll container's automatic min-size is 0). Fix = `min-w-0` on that `div.hidden.lg:block`, or move the class onto it. Orders detail is fixed (docW 1024).
  - **Partial (row 10):** Orders detail "Add Product" drawer left edge fixed on both devices, but on iPhone portrait the page is 482px wide so the drawer's right edge lands at 462px — close ✕ fully off-screen, "Continue" clipped.
  - **Residual, outside the ticket's listed modules:** iPhone portrait detail pages Products (479px), Orders (482px), Shipments (483px) still overflow — culprits are non-wrapping `a.kt-btn` buttons ("Manage in Product Identifiers", "Create Test Deposit Payment", "Create Test Customs Payment") plus Shipments' Financial overview card; `#header` widening is a *consequence* (isMobile layout viewport grows), not the cause. Products/Orders Edit drawers therefore still put the top "Save Changes" partly off-screen (right edge 443/446px).
  - **Measurement gotcha:** a naive "find elements past VW" crawl surfaces the header avatar/notification icons first — ignore the fixed `#header` subtree and walk up from the `<table>`/content element instead.
